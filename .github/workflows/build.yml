name: Build

on: [push]

jobs:
  linux:
    name: Linux
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true

      - name: Build
        run: |
          cargo build --release
          cp target/release/derpc .
          cp config.example.toml .

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: derpc-linux
          path: |
            derpc

  macos:
    name: MacOS
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true

      - name: Build
        run: |
          cargo build --release
          cp target/release/derpc .
          cp config.example.toml .

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: derpc-macos
          path: |
            derpc

  windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true

      - name: Build
        run: |
          cargo build --release
          cp target/release/derpc.exe .
          cp config.example.toml .

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: derpc-windows
          path: |
            derpc.exe
